import {getServerSession} from "next-auth";import {prisma} from "@gymchallenge/database";import {authOptions} from "@/lib/auth";import {fail,ok} from "@/lib/api-response";import {challengeCategorySchema} from "./schema";
async function admin(){const session=await getServerSession(authOptions);return session?.user.role==="ADMIN"?session:null;}export async function POST(request:Request){if(!await admin())return fail("FORBIDDEN","No tienes permisos",403);const parsed=challengeCategorySchema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return fail("VALIDATION_ERROR",parsed.error.issues[0]?.message??"Datos inválidos",422);try{return ok(await prisma.challengeCategory.create({data:parsed.data}),"Categoría creada",201);}catch{return fail("CATEGORY_EXISTS","Ya existe una categoría con ese slug",409);}}
