"use client";
import { useState } from "react"; import { signIn } from "next-auth/react"; import { useRouter } from "next/navigation"; import { appConfig } from "@gymchallenge/config";
export default function Login(){const router=useRouter();const [error,setError]=useState("");const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const fd=new FormData(e.currentTarget);const result=await signIn("credentials",{identifier:fd.get("identifier"),password:fd.get("password"),redirect:false});setLoading(false);if(result?.error){setError("El usuario o la contraseña no son válidos.");return;}router.push("/inicio");router.refresh();}return <main className="grid min-h-screen place-items-center px-4"><form onSubmit={submit} className="card w-full max-w-md space-y-5 p-7"><div><p className="font-bold text-lime-400">{appConfig.name}</p><h1 className="text-3xl font-black">Bienvenido</h1><p className="muted">Ingresa con tu nombre de usuario.</p></div><label className="block">Usuario<div className="mt-2 flex items-center rounded-xl border border-slate-700 bg-slate-950 focus-within:border-lime-400"><span className="pl-3 text-slate-500">@</span><input name="identifier" required minLength={3} maxLength={40} autoCapitalize="none" autoCorrect="off" autoComplete="username" placeholder="santiago" className="w-full bg-transparent p-3 outline-none"/></div></label><label className="block">Contraseña<input name="password" type="password" required minLength={8} autoComplete="current-password" className="mt-2 w-full rounded-xl border border-slate-700 bg-slate-950 p-3"/></label>{error&&<p role="alert" className="text-sm text-red-400">{error}</p>}<button className="btn w-full" disabled={loading}>{loading?"Ingresando…":"Iniciar sesión"}</button></form></main>}
