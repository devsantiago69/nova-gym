"use client";
import { useState } from "react"; import { signOut } from "next-auth/react";
export default function ChangePassword(){const [message,setMessage]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);const res=await fetch("/api/v1/profile/password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({currentPassword:fd.get("currentPassword"),newPassword:fd.get("newPassword")})});const json=await res.json() as {success:boolean;message:string;errors?:Array<{message:string}>};if(!res.ok){setMessage(json.errors?.[0]?.message??json.message);return;}await signOut({callbackUrl:"/login"});}return <main className="grid min-h-screen place-items-center px-4"><form onSubmit={submit} className="card w-full max-w-md space-y-5 p-7"><h1 className="text-3xl font-black">Cambia tu contraseña</h1><p className="muted">Por seguridad debes establecer una contraseña personal antes de continuar.</p><label className="block">Contraseña temporal<input name="currentPassword" type="password" required className="mt-2 w-full rounded-xl border border-slate-700 bg-slate-950 p-3"/></label><label className="block">Nueva contraseña<input name="newPassword" type="password" minLength={12} required className="mt-2 w-full rounded-xl border border-slate-700 bg-slate-950 p-3"/></label>{message&&<p role="alert" className="text-red-400">{message}</p>}<button className="btn w-full">Actualizar contraseña</button></form></main>}
