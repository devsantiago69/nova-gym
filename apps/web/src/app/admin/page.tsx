import { prisma } from "@gymchallenge/database";
export default async function AdminDashboard(){const now=new Date();const month=new Date(Date.UTC(now.getUTCFullYear(),now.getUTCMonth(),1));const [users,activeUsers,attendances,monthAttendances,points,storage]=await Promise.all([prisma.user.count({where:{deletedAt:null}}),prisma.user.count({where:{status:"ACTIVE",deletedAt:null}}),prisma.attendance.count(),prisma.attendance.count({where:{status:"COMPLETED",localDate:{gte:month}}}),prisma.pointLedger.aggregate({_sum:{amount:true}}),prisma.attendancePhoto.aggregate({_sum:{sizeBytes:true}})]);const cards=[["Usuarios",users],["Usuarios activos",activeUsers],["Asistencias del mes",monthAttendances],["Asistencias históricas",attendances],["Puntos emitidos",points._sum.amount??0],["Evidencias almacenadas",`${((storage._sum.sizeBytes??0)/1048576).toFixed(1)} MB`]];return <section><p className="text-sm font-bold text-lime-400">ADMINISTRACIÓN GLOBAL</p><h1 className="text-3xl font-black">Resumen de la plataforma</h1><p className="mb-7 muted">Métricas reales calculadas desde PostgreSQL.</p><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{cards.map(([label,value])=><article key={label} className="card p-5"><p className="muted">{label}</p><p className="mt-2 text-3xl font-black">{value}</p></article>)}</div></section>}
