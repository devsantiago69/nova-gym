import {prisma} from "@gymchallenge/database";import {ChallengeCategoryManager} from "@/components/admin/challenge-category-manager";export default async function Page(){const rows=await prisma.challengeCategory.findMany({orderBy:{createdAt:"desc"}});return <section><p className="text-xs font-black text-orange-300">GAMIFICACIÓN</p><h1 className="mt-1 text-3xl font-black sm:text-4xl">Catálogo de retos</h1><p className="mb-7 mt-2 muted">Crea experiencias, ajusta metas y controla las recompensas sociales.</p><ChallengeCategoryManager initial={rows}/></section>}
